# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test folder and binary, built with `go test -c`
tests/
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Go workspace file
go.work

# macOS files
.DS_Store

# IDE config directories (remove the comment below to include it)
.idea/
.vscode/

# Dependency directories (remove the comment below to include it)
vendor/

# Binary directories, but keep .gitkeep
/.data/bin/*
!/.data/bin/.gitkeep

# Log directories, but keep .gitkeep
/.data/log/*
!/.data/log/.gitkeep


# Log directories, but keep .gitkeep
/.data/data/*
!/.data/data/.gitkeep


# Goreleaser directories
dist/

# Generate files
*_gen.go
